<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand colorx" href="/">SUD</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
        aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav ml-auto">


            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    <i class="fas fa-user"></i>
                    {{#if checkuser}}
                    {{user}}
                    {{else}}
                    User
                    {{/if}}
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    {{#if checkuser}}
                    {{#if checkprofile}}
                    <a class="dropdown-item" href="/users/logout"><i class="fas fa-sign-out-alt"></i> logout</a>
                    <a class="dropdown-item" href="/profile/edit"><i class="fas fa-address-card"></i> Edit
                        profile</a>
                    {{else}}
                    <a class="dropdown-item" href="/profile"><i class="fas fa-address-card"></i> profile</a>
                    <a class="dropdown-item" href="/users/logout"><i class="fas fa-sign-out-alt"></i> logout</a>
                    {{/if}}
                    {{else}}
                    <a class="dropdown-item" href="/users/signup"><i class="fas fa-user-plus"></i> signup</a>
                    <a class="dropdown-item" href="/users/signin"><i class="fas fa-sign-in-alt"></i> signin</a>
                    {{/if}}
                </div>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li>
        </ul>
    </div>
</nav>